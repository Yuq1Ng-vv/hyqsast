# HyqSast 对抗性审查确认清单

> 2026-08-25 · 6 维度独立审查 × 逐条对抗验证（workflow wb4fdpq9r：24 agent / 0 错误 /
> 1.58M token / 44 min，串行内存安全编排，无 OOM）。
> 报告 18 条 → **17 条确认 + 1 条已覆盖（即下文中 M2，先行已记录）+ 0 条反驳**。
> 所有条目已**人工逐行复核**（非仅采信 agent 结论），按影响排序。
> 姊妹文档：`docs/误报面清单.md`（A–F）、`docs/漏报面清单.md`（A–I）、
> `docs/扫描流程排查报告.md`（阶段排查 T/F/FP/FN）。

## 已验证确认项（17 条）

### FP 类

**F1. 重载方法塌缩为单图节点** · `graph.py:518,570`
`_uid(NODE_FUNCTION, path, fn.name)` / `_uid(NODE_PARAMETER, path, fn.name, pname)`
只用裸函数名，不带行号/重载下标。同文件重载（`setValue(int)`/`setValue(String)`）
共用节点 id，networkx `add_node` 合并属性 → 两个方法塌成一个节点，跨文件边错连、
参数节点互串。`graph.py:540-545` 的 `_fkey(name, line)` 已用 `name$line` 消歧但**只
用于索引不用于节点 id**。修复方向：`_uid` 加 `start_line`（或重载下标）参与。

**F2. 参数合成赋值整函数跨度** · `dataflow.py:156-164,184`
Phase 1.5 为每个参数造合成 `_Assign`，`node=func_node`（整个函数节点），
`def_end_line = 函数末行`。参数「定义」横跨整个函数，RHS→LHS 桥接在
`[起始, 末行]` 区间内把参数桥到无关局部变量；`source=ptext` 含注解
（`@RequestParam String fileName`）时注解/类型名也会被当 RHS 匹配。放大 F 类
遮蔽错配。修复方向：参数 def 只覆盖其真实作用域（参数声明点/函数头）。

**F3. `_word_in_text` 字符串字面量内容当词边界** · `graph.py:232-247`
词边界 `(?<![A-Za-z0-9_$])…(?![A-Za-z0-9_$])` 不识别字符串字面量。实测
`_word_in_text('s','String q = "a s b";')==True`、`_word_in_text('id','"SELECT id
FROM t"')==True` —— 字符串里的字符被当变量引用。RHS→LHS（graph.py:1402）与
var_ref→call_site 桥接据此误连。修复方向：剔除字符串/注释字面量后再判词边界。

**F4. Spring 注解提取三形态不全** · `spring.py:225-244`
按 `_SPRING_METHOD_ANNOTATIONS` 插入序（GetMapping→…→RequestMapping）首个命中
即 return。① 多注解叠加（`@GetMapping`+`@PostMapping` 同方法）只出第一个；②
`method={RequestMethod.GET, RequestMethod.POST}` 数组 `_extract_method_attribute`
split 取最后一个元素；③ 裸 `@RequestMapping`（无 method）走默认。HTTP 方法
提取不完整 → endpoint 方法误标（FP）+ 部分漏（FN）。修复方向：收集全部命中
注解、解析数组、裸 RequestMapping 视为全方法。

**F5. `_handler_has_source` 只查 NODE_ASSIGNMENT** · `discovery.py:359-380`
`if data.get("node_type") != NODE_ASSIGNMENT: continue` —— 只对 assignment 的
source 文本匹配 YAML source 模式，从不查 NODE_PARAMETER。而 Spring/Connexion
handler 最常见的 source 是参数（`mark_params_as_sources` 打在参数节点上）。
纯 `@RequestParam` 处理器被误报 `endpoint_no_source`（默认开启的盲区清单大量
FP）。3 个审查员独立命中。修复方向：`_handler_has_source` 同时检查该 handler
的参数节点是否有 taint_source 标签（或按 source 标签而非文本重匹配）。

**F6. 短属性子串 source 模式过宽** · `taint_rules.yaml:3477` + Python sources
Java `injection_general` 有 7 字符的 `params.`（:3477）；Python sources 有
`.data`（:16）、`.body`（:70）、`.filename`（:72）、`.payload`（:80）。
`_label_taint_nodes`（graph.py:1841-1846）对每个 assignment/call_site 节点取整
条语句文本（`source` / `expression`），经 `match_all_sources`（taint_loader.py:
296-299）纯 `pat in text` 子串打 source 标签。任何名为 `params` 的局部变量/字段
（`params.get/put/set` 全中招）、任何 `.data`/`.body`/`.filename`/`.payload`
属性访问（ORM 行、dataclass、`request.payload`）都被标成污点源 → 大量伪 source
放大候选面。注：NODE_PARAMETER 走 `_classify_parameter_source` 按单参数注解分类，
不受此影响——过宽面是 assignment/call_site。修复方向：source 模式要求更精确
token（如 `request.params[`/`request.params.get(`），或带词边界/限定符匹配。

**F7. pattern_sinks 去重只覆盖 weak_crypto** · `analyzer.py:93-95,406-411`
`_PATTERN_ALIAS_CATEGORIES` 只映射 `weak_crypto→crypto_weakness` 一档，`covered`
去重（406-411）对 `cat` 查 `_PATTERN_ALIAS_CATEGORIES.get(cat, ())`，其余 pattern
型类别拿到空元组 → 永不跳过。而 `MessageDigest.getInstance("MD5"` 同时列在
`crypto_weakness`（taint 型）与 `insecure_hash`（pattern 型）sinks；`new Random(`
/`java.util.Random` 同时列在 `crypto_weakness` 与 `weak_randomness`。节点同时命中
pattern 型与其 taint 型同类 sink 且收到 taint 流时，产出**同节点重复 finding**
（同一弱算法报 crypto_weakness + insecure_hash / weak_randomness 两条）。BUG 49
的注释只处理了 DES 的 weak_crypto 一档。修复方向：把
`insecure_hash→crypto_weakness`、`weak_randomness→crypto_weakness` 补进
`_PATTERN_ALIAS_CATEGORIES`（需回归确认不误伤——`MessageDigest.getInstance("MD5")`
不带 taint 时本就不该有 taint 型 crypto_weakness，pattern 型 insecure_hash 应保留）。

### FN 类

**N1. 位置键去重折叠同函数不同参数 source** · `analyzer.py:319-325`
去重键 `(cat, source.file_path, source.line, ...)`，而参数节点无
`line`/`start_line` 属性，`_node_fields`（683）兜底取 `_line_of(location)` =
函数起始行（graph.py:577 打 `location=函数起始行`）→ **同函数所有参数的
source.line 相同**。两参数流到同一 sink 时去重键塌缩，一条被静默丢弃。
修复方向：去重键加参数名/参数位置（或节点 id）。

**N2. JAX-RS 提取器死代码** · `frameworks/__init__.py:42-54`
`jaxrs.py` 完整实现 `JaxRsExtractor`（@Path/@GET、@PathParam/@QueryParam、
鉴权注解），但 `__init__.py` 只 import/_register 六个框架，从未注册 jaxrs。
JAX-RS 应用接口维度全漏。修复方向：补一行 `_register("jaxrs", JaxRsExtractor)`。

**N3. 源循环早退饿死未出现类别** · `analyzer.py:297-300`
早退条件 `all(c >= max for c in per_category.values())` 只查「已见类别」——
若某类别 source 在 `source_ids` 迭代顺序后面、且前序类别全封顶，break 饿死
未出现类别（该类 finding 归零）。修复方向：检查全部类别
（`per_category.get(cat, 0)` 含 0）。

**N4. `$`-元变量模板模式是死规则** · `taint_rules.yaml` sql_injection sinks
94 条中有 **25 条是 CodeQL 风格模板**（`String $VAR = "$SQLSTR";`、
`(Connection $C).prepareStatement(...).$SQLFUNC(...)`、
`(JdbcTemplate $T).$M($A,...)`、`"$SQLSTR".concat(...)`、`(String $A)` 等），
但全代码库**没有任何模板展开/正则转换**——`match_sink`/`match_all_sinks`
（taint_loader.py:312/323）全是字面子串 `pat in text`，含 `$VAR`/`$SQLSTR` 的
模式对真实代码文本**永远匹配不上**。凡指望这些模板抓住的形态（内联字符串拼接
进 SQL、链式 `prepareStatement(...).$SQLFUNC(...)`、`StringBuilder` 拼接）全部
静默漏报，规则库实际召回远低于表观覆盖。修复方向：删除 25 条死模板，或为模板
实现轻量正则翻译（`$VAR`→`[^;"]+`）——后者需要回归验证真实召回不降。

### 内存类（MEM）

**M1. BFS 路径全量物化** · `analyzer.py:517-574`
`_bfs_to_sink` 把 `results` 全量物化（530），每 sink 存 5 条深度 ≤20 的
(node_path, edge_path) 列表，每次扩张 `[*node_path, succ]`/`[*edge_path, etype]`
整列表拷贝（568/572），**不引用类别预算**（50 在 303 行外层才裁）。稠密可达区
（70k 项目每源达 ~42 sink）单源物化数百条路径 × 数千源 = 峰值内存尖峰。
修复方向：BFS 内早停（预算满即停扩）或懒迭代（yield）。

**M2. 报告序列化全量深拷贝** · `schema.py:300-322`
`to_dict()` 用 `asdict(self)` 深拷贝整个 ScanResult（含全部 findings/链），
`to_json()` 一次性 `json.dumps` 整串再 write_text。3.67M findings 时峰值 =
对象 + 深拷贝 + JSON 字符串三份内存。`to_elements_json` 同样
`[asdict(e) for e in taint_elements]`。修复方向：流式序列化 / 分批 asdict。

### 性能类（T）

**T1. `_collect_taint_elements` 全图节点前置求值** · `analyzer.py:1004-1013`
对全图**每个**节点（含无标签节点）先调 `_node_fields`（683，无 code 时文件读回
兜底 692-693）再判标签。与 T13（模式重扫）叠加。修复方向：先判标签再求值字段，
或按标签索引遍历。

**T2. `_source_files` 无 memo 全量 rglob** · `analyzer.py:1102-1112`
每次 `sorted(self.directory.rglob("*"))` + 逐文件 `is_file()` + `detect_by_extension`，
`_extract_endpoints` 与 `_summarize` 各调一次。70k 文件每次全目录遍历排序。
修复方向：lru_cache 或复用建图阶段的文件清单。

### 缓存类（CACHE）

**C1. 缓存 TOCTOU——建图中途新增文件永久漏扫** · `graph.py:891-899,1011-1014,854`
`add_directory` 的 `index_entries` 在建图开始**一次性物化**（891-899），只有这些
条目进 `add_file` 建图；而写缓存前 `_compute_source_fingerprint(root)` **重新
rglob 整个目录**算当前全量指纹（1011-1014）。若建图期间（分钟–小时级的长时构建）
目录里新增了可识别语言文件（Maven generate-sources 生成 .java、CI 并发、增量
checkout、后台脚本写文件），它**不进图但进存盘指纹**。下次运行指纹相等
（854）→ 直接恢复缓存 → 该文件**永久不被扫描**（确定性缺陷：同一目录两次运行
结果因建图期间外部写文件而不同）。修复方向：指纹基于建图实际使用的
`index_entries`（或建图前后各拍一次清单做差集），而非写缓存时重扫全目录。

**C2. 缓存指纹范围过宽——非扫描语言文件改动即触发全量重建** · `graph.py:497,896`
`_compute_source_fingerprint` 源文件筛选是 `detect_by_extension(entry) is not None`
（497，覆盖**全部已注册语言** java/python/javascript），而建图 `index_entries`
筛选是 `detect_by_extension(entry) in self._parser.providers`（896，**只含本次
`--language` 配置的语言**）。指纹集 ⊋ 图文件集：混合语言仓库按单语言扫描
（`--language java`）时，任意 .py/.js 文件的改动/增删都会使指纹失配 → 每次都
全量重建，缓存形同虚设（性能缺陷，且随仓库语言杂度放大）。修复方向：指纹筛选
条件与 `index_entries` 对齐为 `in self._parser.providers`。

## 外部审查补充（用户转述其他 AI 发现 · 逐条人工验证）

> 2026-08-25 · 4 条外部发现已逐一对照 taint_rules.yaml / analyzer.py / graph.py 验证。

**E1. CodeQL 模板残留裸类型名 sink** —— `Result(`（java/xss）、`Process(`
（java/command_injection）**属实且未删**：CodeQL `$TYPE` 模板被压平成纯字串后，
`List(`/`Object(`/`ResultSet(` 等已由 A 类删除（64c7979），但这两个残留。`File(`
（java/path_traversal）A 类审计有意保留（zip slip/`new File(` 覆盖），但会匹配
`getFile(`/`createFile(`/`deleteFile(` 等方法名子串，**存疑待再审计**。`Validator(`
**不实**——全库唯一匹配是精确 FQN `django.core.validators.URLValidator(`。
`Query(`/`Path(` 存在于 python **sources**（5+1 处），疑似针对 FastAPI `Query()`/
`Path()` 校验器（真用户输入），需实证看匹配面。修复方向：删 `Result(`/`Process(`；
再审计 `File(`；python `Query(`/`Path(` 实证后再定。

**E2. log_injection 泛方法名 sink** —— `['.info(', '.debug(', '.warn(', '.error(',
'.trace(', '.fatal(', '.log(']`（java/log_injection）**属实**：纯子串匹配任何对象
同名方法（`logger.info` 之外的任意 `.info(` 都成 sink），极宽。修复方向：B 类
泛方法名收窄——改为 `logger.info(`/`log.info(`/`LOG.info(` 等带前缀模式，或按
sink 上下文限定；直接删会丢 log_injection 召回，须回归。

**E3. source `.getHeader(` 过宽** —— java sources 多处 `.getHeader(`/`.getHeaders(`
**属实**：纯子串匹配任何对象的 getHeader（`response.getHeader` 设置头也被标成
用户输入源）。修复方向：收窄需兼顾 request 对象变量名不固定（FN 风险），可
考虑排除常见 response/responseHeader 对象，或按参数传递上下文判断。

**E4. BFS 不分 confidence 边** —— `_bfs_to_sink`（analyzer.py:588）只读边类型
不读 `confidence`（graph.py:1178/1195 的 high/medium/low）**属实**：low 兜底全
连接边与 high 参数名匹配边同等扩张 → FP 放大。**修复不能简单砍 low 边**（
full-connect 兜底是铁律保召回用的过近似，砍了会 FN）。正确方向：E 类上下文
敏感桥接——路径优选（有 high 不连 low）或 finding 级置信度排序，而非遍历门控。

## 修复落地状态

### 已落地（2026-08-25，OWASP 全量回归通过已提交）

| 项 | 改动 | 验证 |
|---|---|---|
| **C2** | 指纹过滤 `is not None` → `in self._parser.providers`（graph.py） | 缓存行为测试：改非扫描语言 .py 仍命中、改 .java 重建 ✓ |
| **C1** | 写缓存指纹基于 `index_entries` 快照而非重扫全目录（graph.py） | 同上 ✓ |
| **T2** | `_source_files` 加实例缓存（analyzer.py，init 后 directory/language 不变） | 五基准零丢失 ✓ |
| **T1** | `_collect_taint_elements` 标签先行再求值字段（analyzer.py） | 五基准零丢失 ✓ |
| **N4** | 删 sql_injection 94→69 条 `$` 死模板（25 条，YAML 3542–3570 区） | 零命中证明（470 文件 0 命中）+ 五基准零丢失 ✓ |
| **F7** | `_PATTERN_ALIAS_CATEGORIES` 补 `insecure_hash`/`weak_randomness`→`crypto_weakness`；去重时被让位类别折算进 taint finding 的 `related_categories` | 见下「F7 去重实证」✓ |

五基准 A/B（BEFORE=64c7979 之后 / AFTER=本次改动）：vfa 12 / flask-xss 16 / vampi 34 /
demo-java 6 全部 0 丢失 0 新增。

**OWASP per-file 全量回归（results/2026-08-25-fixes-perfile）——铁律闸门通过 ✅**

- `score.txt` 与 A-class 基准（results/2026-08-25-Aclass-perfile）**逐字节一致**：
  TOTAL TPR 96.5%（TP 1365 / FN 50）不变；crypto 100%、hash 69%、weakrand 100% 全不降。
- 全量 findings **24022 → 23004（-1018）**，指纹对称差：**仅候选有 = 0**（零新增 → 零 FN）。
- F7 去重 574 指纹（weak_randomness 472 + insecure_hash 102，全部是被让位类别）：
  866 条折算进同 sink 位置 taint finding 的 related_categories；152 条由同 sink 存活同类
  finding 承接（候选同位置仍有 `insecure_hash`，related 含 crypto_weakness）。
  **机制不变式：`covered` 由 `findings_by_pos` 的 vuln_type+related 构建** —— covered 含别名
  ⟹ 该位置必有 finding 携带该 credit，理论上不存在静默丢 credit 路径；score 逐字节一致为
  最终实证。

### 未落地（需用户指示，均会改变 findings 面，须各自 OWASP 回归）

- **F6** 短属性 source 收窄（`params.`/`.data`/`.body`/`.filename`/`.payload`）——FP 收窄，FN 风险最高
- **F1** 重载方法 `_uid` 塌缩（加 start_line）——图结构修复，改动面大
- **F2** 参数合成赋值整函数跨度（数据流桥接修复）
- **F3** `_word_in_text` 剔除字符串/注释字面量
- **F4** Spring 注解提取三形态；**F5** `_handler_has_source` 查参数
- **N1** 去重键加参数名（FN 修复，加 finding）；**N2** 注册 jaxrs；**N3** 源循环早退查全类别
- **M1** BFS 早停（会改变保留的 5 条路径）；**M2** 报告流式序列化（纯内存）

落地铁律不变：每条落地前跑五基准 + OWASP 回归（真实漏洞零损失）；收窄/删规则类
（F6/F7/N4）还要证明 OWASP TPR 不降、全量 findings 只减不增。

### 规则库过宽审计（2026-08-25 · 语料实证推进中）

审计工具 `perf_probes/rule_width_audit.py`（逐行静态分类）+
`perf_probes/rule_width_match.py`（真实语料命中实证）。规则总数 **4068**：
DEAD 562（`$`/`...` 死模板，**永不命中**）· TYPE 144（裸类型名）· METHOD 275（泛方法名）
· SHORT 630 · WORD 245 · OK 2480。

语料实证（ureport2 469 文件 + OWASP testcode 2740 + 五基准）关键结论：
- **`.getHeader(`/`.getParameter(` 等 Servlet getter source**：request=514 / response=0 ——
  E3「过宽」**实证不成立**，收窄反丢 OWASP header 测试召回 → **保留**。
- **xss 的 `.setHeader(` sink**：462 文件命中全是 `setHeader("X-XSS-Protection","0")`
  （OWASP 漏洞设置行，非 sink）→ **候选从 xss 删除**（header_injection 已覆盖 setHeader）。
- **`File(`（path_traversal）**：753 文件 1290 次，ureport2 样例 12/12 是
  `deleteReportFile(`/`getFile(`/`saveReportFile(` 方法名子串，仅 `new File(tempDir)` 是真 sink
  → **候选收窄为 `new File(` + `java.io.File(`**。
- **`SqlConnection(`（sql_injection）**：154 命中全是 `DatabaseHelper.getSqlConnection()` 方法名，
  无实参无流向产不出 finding → **候选删除**（C# 类型误入 java 规则）。
- **`Random(`（crypto_weakness）**：164/357 是 `SecureRandom(` 构造，但 OWASP 把
  `new java.security.SecureRandom()` 同时当 expected-weak(12) 与 expected-secure(8)，
  纯子串无法区分 → **保留**（收窄丢 weakrand TPR）。
- **`.getWriter(`（xss）**：7207/7213 命中 `response.getWriter(`，**精准保留**。
- **log_injection `.info(`/`.debug(` 等 7 条**：语料 0 命中、零基准覆盖 → E2 收窄候选
  （B 类泛方法名前缀化），需回归。
- `.read(`（xxe）625 文件、`.update(`/`.execute(`（sql）236/207 文件：见审计推进中。

**收窄批次落地（2026-08-25 · `perf_probes/rule_narrow_batch.py` 外科手术）**：

| 变换 | 计数 | 依据 |
|---|---|---|
| DEAD 删除（含 `$`/`...` 模板 + YAML 坏行） | 562 | 纯子串匹配下永不命中（无漏洞场景可证） |
| `File(` → `new File(`/`java.io.File(`/`FileOutputStream(`/`FileInputStream(` | 1 | 753 文件方法名子串 FP |
| xss `.setHeader(` 删（header_injection 保留） | 1 | XSS 是响应体回声，header 非 XSS 场景 |
| xxe `.read(` 删 | 1 | 裸 `.read(` = InputStream/Reader 泛 I/O 非 XML 解析 |
| log_injection 7 方法 × 4 接收者前缀 | 7→28 | 语料 0 命中 + 零基准覆盖 |
| Servlet 源 getter 泛方法 → request./req. 等 | 73 | 每条按实际漏洞面定接收者集 |

**「收窄优先，删除从严」逐条场景证明**（非 DEAD 裸删 4 条，均实证无对应漏洞场景）：
- **`Process(`**（cmdi）：Java `Process` 为抽象类不可 `new`；真实 sink 已由
  `Runtime.exec(`/`Runtime.getRuntime().exec(`/`ProcessBuilder(`+方法全覆盖 → 无独立场景。
- **`SqlConnection(`**（sqli）：C# 类型误入 java 块（JDBC 用 `Connection`），154 命中全是
  `getSqlConnection()` 方法名子串 → 无 java 场景。
- **`Result(`**（xss）：语料命中全是 `DataResult(`/`DriverResult(` 等**其他标识符子串**，
  无一处真 `Result` 输出 sink；真实 xss sink 由 `print(`/`write(` 等覆盖 → 无场景。
- **`.setHeader(`**（xss）：header 不是 XSS 输出面；header_injection 已保留自身 `.setHeader(`。
- **`.read(`**（xxe）：泛 I/O 读非 XML 解析；`SAXReader.read(`/`DocumentBuilder.parse(`/
  `SAXParser.parse(`/`TransformerFactory.transform(` 等具体模式已保留。

**收窄替换的 OWASP 召回风险语料验证**（防 FN，全部 100% 覆盖）：
- getter 接收者分布（OWASP testcode 2740 文件）：`.getParameter(` request 281+req 257、
  `.getHeader(` request 514+req 257、`.getHeaders(` request 400+req 262、`.getQueryString(` 295、
  `.getRequestURI(` 776、`.getRequestURL(` 312、`.getSession()` 1112（无 `getSession(false)` 变体）、
  `.getInputStream(`/`.getCookies(` 全 `request`——窄集 `request./req.` 100% 覆盖；
  `.getReader(`/`.getPart(`/`.getPathInfo(`/`.getServletPath(`/`.getServerName(`/`.getRealPath(`/
  `.getSubmittedFileName(`/`.getOriginalFilename(` OWASP 中 0 命中（无风险）。
- HttpServletRequest 变量名 OWASP 100% 是 `request`（7279 处无其他名）。

**OWASP 回归闸门通过（2026-08-25-narrow-batch，per-file）**：11 类别 TP 与基准
d5313ee 逐字节一致（TOTAL TP1365/FN50，TPR 96.5%），findings **23004→21281
（净减 1723，全为 FP 收缩）**，cross 假路径 15609→14164。收窄批次对真实漏洞
召回零损失，误报面再缩一档。

**demo-java path_traversal「A/B 丢失」= 假报警实证**：OLD 规则在
`return readFile(filename)`（line 69）因 `File(` 子串命中 `readFile(` 调用点节点，
误标 sink=69（链上还有 67→69 假路径）；真 sink 在 line 73。收窄后调用点误标消失
（**实证砍掉的方法名子串 FP**），真 sink 归因从 69 修正到 73。五基准「丢失=1」
正是这条 FP，真实召回未动。收窄后续，OWASP 必需集
`java.io.File(`（子串覆盖 `new java.io.File(`）`+ new File(` `+ FileOutputStream(` `+ FileInputStream(` 全部保留。

### 规则库收窄批次 · python 侧（2026-08-25 · `perf_probes/rule_narrow_python.py`）

**删 4 条（实证无场景/纯 FP，语料命中全是非用户输入）**：
- `.endpoint`（sources/ssrf）——路由端点是开发者定义；命中全是 `app.url_map.iter_rules()`
  路由表内省 → **连带砍掉 flask-xss 2 条实证 FP finding**（sql「注入」实为
  `' '.join()` 字符串 join、xss「注入」实为渲染自己的静态索引页）
- `.join(`（sinks/sql_injection）——字符串/os.path join，非 SQL 执行
- `url_for(`（sinks/open_redirect）——URL 生成不重定向，真 sink 是 `redirect(`
- `render(`（sinks/xss）——与 `render_template(`/`render_template_string(` 重复；
  命中 MethodView.render（安全模板渲染非 XSS）

**收窄 41 处（receiver 前缀，语料实证 `request` 是唯一真实接收者）**：
- `.get_json(`→`request.get_json(`/`req.get_json(`（10 类 sources）
- `.headers[`/`.headers.get(`→`request.` 前缀（命中 `response.headers` 为响应头 FP）
- `.url`→`request.url`/`req.url`（ssrf；`sqlalchemy.url` 配置是 FP）
- `.data`/`.body`/`.payload`→`request.` 前缀（F6 短属性收窄）
- `.json()`→`request.json()`/`req.json()`（vfa 的 `request.json` 属性是独立模式不受影响）
- `Authorization`→`request.headers.get('Authorization'`/`request.headers['Authorization'`
  （auth_bypass；命中响应头设置是 FP）
- `state`→`request.args.get('state'`/`request.form.get('state'`（auth_bypass；命中
  `with_statement` 子串是 FP）

**保留（真实场景不可窄/收窄反丢召回）**：`.read(`（vfa xss 真源 `yfile.read()`）、
`.filename`（Flask 上传文件名接收者不定）、ORM 族 `.filter(`/`.order_by(`/`.select(`
等（SQLAlchemy 允许传原始字符串 `.filter("raw %s" % x)` 是真注入场景，留作未审计项）、
`Response(`/`callback`/`getattr(`/`redirect(`/`.execute(`/`text(`。

**五基准 A/B**：vfa 12、vampi 34 **全保留 0 丢失**；flask-xss 16→14，丢的 2 条
均实证为上述 `.endpoint`/`.join(` FP（路由表源 + 字符串 join 源）。java/javascript
规则零变动。

---

## 低风险批次落地（2026-08-25 · M2 / F5 / N2 / N3 / F4）

对抗审查清单中 11 个「未完成」项按风险分组，本次落地**低风险五件**（纯内存 / 盲区
修复 / 一行注册 / 逻辑修复 / 接口标注），每件经功能测试 + 五基准 A/B 证明「真实漏洞
一条不少」。OWASP 全量回归见文末。

### M2 · 报告流式序列化（`schema.py`）—— 纯内存，输出字节不变

旧实现 `json.dumps(to_dict())` 同时物化三份内存：`asdict` 深拷贝的整棵结果树、
完整 JSON 字符串、落盘字符串本体——百万 findings 报告在 1.6GB 机器直接 OOM。
新增 `_stream_dump` 流式写出器（`_stream_value`/`_stream_list`/`_stream_dict`），
逐字段写盘，任意时刻只持一个叶子标量；**输出与 `json.dumps(v, ensure_ascii=False,
indent=2)` 字节级一致**（6 份报告全量比对通过）。`to_json`/`to_flat_json`/
`to_canonical_json`/`to_elements_json`/`to_canonical_route_json`/
`to_canonical_agg_json` 六处 path 分支全部改流式（path 给定时返回空串，仓库内
无调用方使用返回值；path 为 None 保持旧全串行为）。

### F5 · `_handler_has_source` 补参数节点（`discovery.py`）—— 盲区误报修复

原实现只查 `NODE_ASSIGNMENT` 是否命中 YAML source 模式；纯 `@RequestParam`
等注解 handler 若函数体无赋值，被误报 `endpoint_no_source`。现补查
`NODE_PARAMETER` 节点的 `taint_source` 标签（graph.py `_label_taint_nodes`
的 NODE_PARAMETER 分支，注解型 source 走 `_classify_parameter_source` 精确分类）。
**纯加性**：只会把误报的 endpoint_no_source 消掉，不会新增盲区。当前规则下
java sources 里的裸 `@RequestParam`/`@PathVariable` 等注解模式（各 5-9 类别）
会让参数声明文本命中，掩盖了本场景；F5 是「规则一收窄就生效」的配套完备性修复。

### N2 · 注册 jaxrs 提取器（`frameworks/__init__.py` + `analyzer.py`）—— 接口维度 FN 修复

`JaxRsExtractor` 已存在但未注册（死代码），JAX-RS 应用接口维度全漏。补两处：
`_register("jaxrs", JaxRsExtractor)` + `_LANGUAGE_FRAMEWORKS["java"]` 加入
`"jaxrs"`（光注册不生效，需进 java 默认提取器表）。实测 `@Path("/users")` +
`@GET`/`@POST` 正确提取 `GET /users/{id}` / `POST /users/`（framework=jaxrs）。
OWASP testcode 无任何 `javax.ws.rs`/`jakarta.ws.rs`/`@Path` 用法（grep 实证）→
对 OWASP 中性。每 java 文件多一次 `detect()`（解析+注解扫描），成本有界。

### N3 · source 循环早退查全类别（`analyzer.py`）—— FN 修复（findings 增加）

原 `all(c >= max for c in per_category.values())` 只查**已见**类别：source 排序
导致某类别先饱和时提前 break，排在后面的类别整类饿死。改为对「图里存在 sink 的
全部类别」判断（`target_categories`，未产出类别 `get()=0 < cap` 不会 break）。
**因果实证**：cap=1 双路由项目（sqli + open_redirect 两 sink），旧代码只出
`{open_redirect:1}`（sqli 被饿死），新代码 `{open_redirect:1, sql_injection:1}`
两类别齐活。默认 cap=50 的小基准永不命中 → 五基准零差异；大扫描（70k）上修复
真 FN。

### F4 · Spring 注解三形态（`spring.py`）—— 接口方法标注修复

原 `_find_route_annotation`：①多注解叠加只出第一个；②`method={POST, GET}`
数组取最后一个；③裸 `@RequestMapping` 默认 GET。重写为：
① 收集全部映射注解方法的并集（去重保序）；② `_extract_methods_attribute` 走
`_walk_subtree` 找 `element_value_pair`（key 是首个命名 child，tree-sitter java
对 element_value_pair 无 `name` 字段）+ `re.findall` 解析数组全部元素；
③ 裸 `@RequestMapping` → `_ALL_SPRING_METHODS`（GET/POST/PUT/DELETE/PATCH/
HEAD/OPTIONS）。路由取第一个映射注解。功能测试六形态全对
（bare/数组/单值/叠加/GET/POST/PATCH），返回类型 `tuple[str,str]` →
`tuple[list[str],str]`，两个调用点同步改。

### 五基准 A/B（HEAD vs 全部改动，含 jaxrs 默认）

vfa 12 / flask-xss 14 / vampi 34 / demo-java 5 / probe×2 —— **丢失=0 新增=0 全绿**。

### OWASP 全量回归（`results/2026-08-25-lowrisk-batch/`）

**与 java 收窄后基线 `2026-08-25-narrow-batch` 逐行完全一致**：TPR 96.5%
（TP 1365 / FN 50 不变）、findings 21281（不变）、cross 14164（不变）、
每类别 TP/FP/FN 全同。低风险批次对 OWASP 零损失零变化——M2 纯序列化、
F5 只碰盲区、N2（jaxrs 默认）实证 OWASP testcode 无任何 JAX-RS 用法、
N3（cap=50000 永不命中）、F4 只碰接口方法标注，均不影响 findings 面。

---

## F6-java 精准收窄批次（`taint_rules.yaml`）

**背景**：F6 清单点名 java `injection_general` 的 7 字符 `params.`（原
`taint_rules.yaml:2964`）与 Python 短属性——本批只做 java 半边。量化探针
（2740 个 OWASP testcode + demo-java 文件，按文件建图取 production 同款
`source`/`expression` 文本）：**`params.` 图文本 0 命中 + 原始源码 0 出现**；
裸注解仅 `@RequestParam` 命中 assignment 节点（demo-java DemoController
6 个形参，各被标 10 类 source）。

**根因**：NODE_PARAMETER 走 `_classify_parameter_source`（graph.py 精确
单参数分类），**与 YAML 子串无关**；但建图层为形参另建了 NODE_ASSIGNMENT
节点（文本 `@RequestParam String id`），经 `match_all_sources` 纯子串
匹配被标成 **10 类** source（code_injection/command_injection/…/xxe）。
YAML 裸注解是叠加在精确路径之上的过宽标签。BFS 从全部 `taint_source`
出发、**不按 source 类别门控**（analyzer `_build_findings`，vuln_type 由
sink 决定），故收敛 source 类别不剪链。

**改动**（只动 java sources，sinks/sanitizers/其他语言 字节级不动）：
- **17 个裸注解收敛到分类器目标类别**：`@RequestParam`→injection_general、
  `@PathVariable`/`@PathParam`→path_traversal、`@RequestPart`→deserialization、
  其余→injection_general（`@RequestHeader`/`@HeaderParam` 分类器给
  header_injection 但 java sources 无此类别→兜底 injection_general；
  `@SessionAttribute` 原只挂 xss→迁入 injection_general）。删 70 行
  非目标类别重复，保留 12 行目标，补 1 行。
- **`params.` → `request.params.` + `req.params.`**（接收者限定）。零命中
  实证 + 所有真实 java 请求参数场景（`request.getParameter*`、
  `.queryParams(`/`.params(`、裸注解）均已被精确模式覆盖。

**验证**：
- 五基准 A/B：vfa 12 / flask-xss 14 / vampi 34 / demo-java 5 —— 丢失=0
  新增=0 全绿（demo-java 6 个 `@RequestParam` 链经 NODE_PARAMETER 分类器
  + F5 兜底，一条不丢）。
- demo-java blind_spots = 0（不变）。
- OWASP 全量回归（`results/2026-08-25-f6java-batch/`）：评分逐类别与
  `2026-08-25-narrow-batch` 全同（TPR 96.5%、TP1365/FN50、findings 21281、
  cross 14164），`compare_findings.py` 逐行指纹 A/B **零差异**。

## F6-python 精准收窄批次（`taint_rules.yaml` · commit `032bcd5`）

**背景**：F6-java 批次收尾后，Python 半边只剩「无接收者框架访问器」一类
过宽 source。量化探针（vfa / flask-xss / vampi 39 个 python 文件按文件建图，
取 production 同款 `source`/`expression` 文本做纯子串匹配）：

- `.args.get(` / `.form.get(` / `.cookies.get(` 的**全部命中都带 `request.`
  前缀**（真输入）；裸模式在野外也会打 `obj.args.get(` 这类非请求对象
  （字典/ORM/自定义类同名属性）→ 伪 source。
- `.json` / `.FILES` / `.META` / `.match_info` / `.query_params` / `.values.get`
  等语料 0 命中——真实框架用法都是 `request.` 接收者，收窄零损失。
- **`.filename` / `.read(` 是承重真输入，必须保留**（F6 清单曾疑其为过宽）：
  vfa 上传链 `f = request.files['file']` → `f.filename` → `f.save(path)` 的
  path_traversal / XXE，以及 `yaml.load(yfile.read())` 的 deserialization /
  xss 全靠 `.filename`/`.read(` 起链；`.read(` 那条 deserialization@327→329
  与 xss@327→331 **无 sibling 链，是唯一 source**。砍掉即真实漏洞 FN。
- `.cleaned_data.get(` 接收者是 Django form 对象（非 request）→ 保留。

**改动**（只动 python sources，其余字节级不动）：**19 个无接收者访问器 →
`request.X` + `req.X` 双接收者限定**（与既有 `.headers.get(`/`.get_json(`
同风格），每处拆两行：`.args[`/`.form[`/`.json[`/`.cookies[`/`.FILES[`/
`.FILES.get(`/`.META.get(`/`.args.get(`/`.form.get(`/`.json.get(`/
`.json_data[`/`.json_data.get(`/`.match_info[`/`.match_info.get(`/
`.query_params[`/`.query_params.get(`/`.values.get(`/`.cookies.get(`/
`.get_signed_cookie(`。共 108 处 → 216 行，残留裸模式校验为零，
PyYAML 解析 OK、python source 类别数 15 不变。

**验证**：
- 五基准 A/B（`/tmp/ab_f6py_before|after`）：vfa 12 / flask-xss 14 / vampi 34
  / demo-java 5 —— 丢失=0 新增=0 全绿。
- OWASP：java 区块 diff 字节级为零（hunk 全部在 python 区块）；
  `taint_loader.rules_for` 按语言隔离（`self._data.get(language)`），python
  规则不可能加载进 java 扫描；28 文件跨 CWE 冒烟（`/tmp/owasp_smoke`，
  覆盖 0001–2700 全编号区间）before/after **176 findings 字节级一致**。
- 承重真输入（`.filename`/`.read(`）经 vfa 上传链在五基准 A/B 中原样命中，
  实证零 FN 损失。

## P1-8 宽 sink 接收者限定收窄批次（java · `taint_rules.yaml` · 本批）

**背景**：用户在 **700 万行 Java 项目**（79 万函数 / 5 万 sink 点 / 2 万 source 点 / 7 万文件）上实测，暴露一批裸泛方法名 sink 规则**匹配到大量正常代码**：`.execute(` 5000 处（Axis2/HttpClient/Executor）、`.getMethod(` 2169、`Class.forName(` 2689、`Result(` 5769、`.compile(` 754、`putValue(` 902……用户原话（铁律）：**「sink 点确实能识别到真实的 sink 点，但他也会匹配到正常代码呀，噪声太多这个问题会彻底让这个工具无用。」** 收窄原则从「事后 sink_excludes 打补丁」升级为「**接收者限定正向匹配**——正常代码根本不匹配，不是靠排除」。

**锚点实证**（f6java-batch 基线 merged 20751 条按模式统计）：

| 模式 | OWASP 锚点 | 证据 | 处置 |
|---|---|---|---|
| `.execute(` | 448 全真 SQL | 接收者 `statement`×406 + `JDBCtemplate`×42，无 executor/scheduler | 接收者限定后全保留（`statement.execute(`/`jdbcTemplate.execute(`/…） |
| `.query(` | 78 全真 | 全 JdbcTemplate | 接收者限定保留（`jdbcTemplate.query(`/`jt.query(`/…） |
| `putValue(` | 173 全真 | 全 `getSession().putValue(` | 限定为 `getSession().putValue(`/`HttpSession.putValue(` |
| `.evaluate(` | 126 全真 XPath | — | 承重保留 |
| `.compile(` | **0** | 死模式 | 删 |
| `.delete(`/`.update(`/`.insert(`/`.select(`/`Class.forName(` | **0** | 700 万行项目里全是 File.delete / MessageDigest.update / StringBuilder.insert / Reactive/LINQ | 接收者限定或删 |
| `Method.invoke(`/`.getMethod(`/`MapContext`/`NashornScriptEngine` 等反射族 | **0** | 反射非注入 | code_injection/injection_general 删 |

**改动**（只动 java 区块）：sql_injection 裸 `.execute(`/`.query(`/`.update(`/`.delete(`/`.insert(` → 接收者限定（JDBC `statement.*`/`stmt.*`/`ps.*`/`pstmt.*`/`cstmt.*`/`preparedStatement.*`/`callableStatement.*`/`jdbcTemplate.*`/`jt.*`/MyBatis `sqlSession.*`/`session.*`/`Mapper.*`/`contentResolver.insert(` 等），删 `Class.forName(`/`.getJdbcTemplate(`/`.select(`/13 个裸类型名；code_injection/injection_general 删反射族；trust_boundary `putValue(` 接收者限定；xpath 删 `.compile(`；xss 删 `PrintWriter(`；sink_excludes 删 P1-5 的 md.update/StringBuilder.insert（接收者限定后冗余）。`Statement.*`/`PreparedStatement.*`/JPA/Hibernate/`NamedParameterJdbcTemplate.*`/`DSLContext.*`/`SQLiteDatabase.*` 等限定名族不动。

**验证**：
- **OWASP 忠实重分类**（`/tmp/p18_reclass.py`，规则集 = 内置 `taint_rules.yaml` + `rules/java.yaml`，与 CLI 自动发现一致）：20751 条逐条最长匹配 sink 类别，新旧规则 **0 变化、0 丢标（含 →None）** → 评分逐项不变、零 finding 丢失。
- 五基准 A/B（`/tmp/baseline_p18_before|after`）：vfa 12 / flask-xss 9 / vampi 27 / demo-java 5 / 探针 0 —— **6/6 OK，零丢失零新增**。
- demo-java 0 锚点。
