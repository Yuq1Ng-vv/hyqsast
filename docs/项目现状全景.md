# HyqSast 项目现状全景（2026-08-26 快照）

> 给「改了很多、想重新捡起全局」的自己看的一份导航。不是 README 的替代品——
> 这里按「项目是什么 / 铁律 / 架构地图 / 最近改了啥 / 怎么验证没改坏 / 文档导航 / 当前状态」
> 七层理清，每层末尾给出去哪看细节。

---

## 一、一句话定位

**HyqSast 是一个确定性污点分析（SAST）模块**：输入源码目录 + 语言/框架配置，
输出**接口 / 漏洞类型 / 调用链**三类结构化产物。全 tree-sitter（AST）+ NetworkX（图遍历）
+ 子串/正则匹配，**零 LLM**，可在 CI 直接跑。**追求高召回**——产出是「需人工复核的候选」，
不是最终判定，会有误报。

| 维度 | 现状 |
|---|---|
| 支持语言 | java / python / javascript（引擎级） |
| 漏洞类别 | 28 类（`schema.py` SEVERITY_MAP，含 sql_injection/xss/ssrf/path_traversal/xxe/crypto_weakness…） |
| 框架提取器 | spring / jaxrs（java）、flask / django / fastapi / connexion（python）、express（js） |
| 规则库 | `cpg/taint_rules.yaml` ~4600 行，改规则只动 YAML 不动代码 |

---

## 二、两条铁律（所有改动的裁判）

1. **缺陷平衡铁律**：真实漏洞（FN）一条不能丢，误报（FP）能减就减。
   「不增加漏报」≠「不能减少 findings」——只要能证明某条是误报（桥接假连通/跨文件错配/
   宽 sink 误标/遮蔽错配），就该砍。**任何收窄/切断桥接/位置门控改动，必须先过五基准 A/B
   + OWASP 回归证明「真实漏洞零损失」，才落地。**
2. **内存铁律**：本机 ~1.6GB RAM，曾 OOM。跑大型命令前先评估内存：
   OWASP 全量**分块/per-file 扫描**（275 文件/块 × 10）；性能测量用插桩计时
   （`.perf_probe_bench.py`，纯建图无 BFS 无报告写出）；**74MB merged JSON 一次只 load 一份**
   （对比拆脚本单文件加载 + `comm`）。

---

## 三、架构地图（数据流 + 模块）

**数据流**：`建图 → 提取接口 → 跑污点 → 汇总`

```
CPGGraphBuilder.add_directory → _extract_endpoints → _build_findings → _summarize
```

| 目录/文件 | 职责 | 看什么 |
|---|---|---|
| `src/hyqsast/api.py` | `scan()` 公开门面（薄封装） | 所有配置入口 |
| `src/hyqsast/analyzer.py` | 编排层：建图→接口→污点(BFS)→盲区→汇总 | 核心逻辑在这 |
| `src/hyqsast/schema.py` | 纯 dataclass 数据模型 + SEVERITY_MAP | 6 份报告的字段定义 |
| `src/hyqsast/cli.py` | argparse 命令行入口 | 参数清单 |
| `src/hyqsast/cpg/graph.py` | CPGGraphBuilder：nx.MultiDiGraph + 污点标签 | 打标/桥接/缓存 |
| `src/hyqsast/cpg/taint_rules.yaml` | 规则引擎数据（source/sink/sanitizer） | 改规则只动它 |
| `src/hyqsast/cpg/taint_loader.py` | 规则加载/校验/匹配 | `--vuln-types` 过滤在这 |
| `src/hyqsast/cpg/callgraph*.py` | 单文件/跨文件调用图 + import 解析 | |
| `src/hyqsast/cpg/cfg.py` | CFG + 支配分析 | |
| `src/hyqsast/cpg/frameworks/` | Web 框架路由提取器 | 接口从哪来 |
| `src/hyqsast/cpg/languages/` | 语言适配器注册表 | |
| `src/hyqsast_mcp/` | MCP stdio server（stdout 是 JSON-RPC 信道，不传 progress） | |

**污点传播核心**（`graph.py` / `analyzer.py`）：
- source 只表示「有用户输入」；**精确 vuln_type 由 sink 决定**（BFS 前向命中哪个 sink 类别）。
- 跨函数参数匹配：参数名 → 位置 → 全连接兜底（过近似，保证不漏报）。
- `source 优先于 sink`：节点匹配了 source 就不再评 sink。
- `injection_general` 是兜底类别，与具体类别共存时被丢弃。

**6 份输出报告**（`-o report.json` 自动连带写出）：
主报告 / `.canonical.json`（规范版，人工复核首选）/ `.elements.json`（污点元素清单，排查漏报）/
`.flat.json`（扁平聚合）/ `.canonical.route.json`（接口精简）/ `.canonical.agg.json`（聚合规范）。

---

## 四、最近改动全景（最近 ~30 commits 按主题归类）

这是你最需要的部分。最近一个月实际只干了**四件事**，外加一批文档：

### A. 误报治理（FP 收窄）—— 主战场，findings 从 24571 → 21281
> 规则库是主要误报来源（裸类型名 / 通用方法名 / 无接收者访问器），逐批「收窄不删除」。

| commit | 干了什么 |
|---|---|
| `64c7979` | A 类收窄：删 13 个 java 裸类型名 sink 模式 |
| `3abc99e` | 规则库收窄：562 死模板删 + 81 处泛方法收窄（OWASP 零召回损失） |
| `711812e` | python 收窄：4 删 + 41 处接收者前缀收窄 |
| `190c705` | **F6-java**：17 裸注解收敛到分类器类别 + `params.` 接收者限定 |
| `032bcd5` | **F6-python**：19 无接收者访问器 → `request.X`/`req.X`；`.filename`/`.read(` 承重保留 |
| 文档 | `误报面清单.md`（A–F 分类：sink 打标过宽/规模乘积/汇总性能/桥接放大/def-use 遮蔽错配） |

### B. 性能（大项目 7 万文件：从 ~7h → 线性）
> 误报爆炸的**性质**是规模乘积（O(findings×全图)），逐层索引化清剿二次方。

| commit | 干了什么 |
|---|---|
| `dcad5fd`→`d83f40c`→`d3030db` | 跨文件可达全局反向索引 / 文件索引 / 行索引 / 首名索引 / 单趟遍历 / `_word_in_text` C 级预筛 |
| `fec1746` / `2ba70e7` | 每调用方懒预计算可达集 / `_find_func_node_id` 全图→文件索引（去 O(F×G)） |
| `7207ff9` | 污点传播提速：sink 反向可达预筛 + BFS 走廊剪枝 + 汇总全图扫描消除 |
| `800e072` | 建图/报告三段索引化（T4/T8/T12）+ 删全连接兜底 |
| `05ca579`/`71ec475` | 候选爆炸诊断探针（量化 367 万候选的三层来源） |

### C. 正确性 / 漏报面（FN 与机制修复）
| commit | 干了什么 |
|---|---|
| `d5313ee` | **C1/C2** 缓存指纹修正、**T1/T2** 标签先行/结果缓存、**N4** 删 25 条 `$` 死模板、**F7** pattern 别名（weak_crypto→crypto_weakness 等）折算 related_categories |
| `24a6334` | **M2** 报告流式序列化 / **F5** 盲区补参数节点 / **N2** jaxrs 提取器 / **N3** source 循环早退查全类别 / **F4** Spring 注解三形态 |

### D. 消费侧工具 + 文档
| commit | 干了什么 |
|---|---|
| `3a0a681` | `剩余工作清单.md`（P0–P3 执行索引，本文档的「下一步」看它） |
| `90df903` | **`benchmarks/dedup_findings.py`**：按 source 函数 × sink 函数 × vuln_type 归并复核，OWASP 21281 → 10528（2.0x） |

### E. P0 全部落地 + P1 进行中（2026-08-26）
| commit | 干了什么 |
|---|---|
| `a5da419` / `05ba2dc` | `--vuln-types` 定向扫描（sink 过滤 + source/sanitizer 全保留）+ canonical `source_function` + 文档 |
| `2ef804c` | P0-2 `uncovered_sink` 开关 + P0-3 删 `html.unescape(` 出 sanitizer（含 `rules/python.yaml` 裸 `escape(`） |
| `f0a35fd` | **P1-7** python xss sinks 裸 `escape(` → `unescape(`（假 sink 收窄，五基准 6/6 OK） |
| `dbf5075` | **P1-6** `auth_bypass` 类别重构：python sinks ~60 → 9 条（+csrf_exempt），删认证声明/守卫/密码校验/current_user，留 login_user/login/jwt 族；sink_excludes 只留显式 `verify=False`；五基准 vfa 零丢失、flask-xss 14→9 / vampi 34→27（12 条丢失逐条证明 FP，3 条真绕过保留） |
| `d122ba0` | **P1-4** java 防御函数当 sink → 移 sanitizer：`getCanonicalPath(` 出 path_traversal sinks（查路径非写文件，限定名 sanitizer 已存在）、`hasRole(`/`Subject.hasRole(` 出 auth_bypass sinks（鉴权判断非绕过）→ sanitizers 补 `.hasRole(`；五基准 A/B 6/6 OK；OWASP 最新 java 态 merged 0 锚点（单调删除零 TP 变化）；方向探针 BEFORE 10 → AFTER 6 |
| `5493468` | **P1-5** java sql 泛方法名收窄：sink_excludes 排除 MessageDigest/Mac `.update(`（OWASP 530 条 md.update FP 全清，弱哈希由 insecure_hash 独立检测）+ StringBuilder/StringBuffer `.insert(`；`.execute(`/JdbcTemplate `.query*` 承重保留；五基准 6/6 OK；OWASP 重扫 232 文件残留 0、评分逐项不变（TPR 96.5%），findings 21281→20751 |
| `84711a6` | **P1-8** java 宽 sink 接收者限定收窄（700 万行项目实测）：裸 `.execute(`/`.query(`/`.update(`/`.insert(`/`.delete(`/`.select(`/`.compile(` + 反射族（`Class.forName(`/`.getMethod(`/`Method.invoke(`/`MapContext`）在真实大项目匹配到 Executor/HttpClient/MessageDigest/StringBuilder/File/Pattern.compile 等**正常代码**，噪声让工具在真实项目失效；改为**接收者限定正向匹配**（`statement.execute(`/`jdbcTemplate.query(`/`getSession().putValue(`），正常代码根本不匹配；OWASP 20751 忠实重分类（内置+rules/java.yaml）**0 变化 0 丢失**；五基准 A/B 6/6 OK |
| `a5a15f5` 起多批 | **规则库全面审查**：4 agent 并行审内置 + `rules/*.yaml`（java/python/javascript/go/php），按漏洞类型逐类落地——死规则删（子串引擎永不命中的 `(.*` 正则字面量）、错误规则纠（setTimeout 定时器非 code_injection、url.parse 非反序列化、`path.resolve/join/normalize` 非 path_traversal sanitizer、`Response(x, mimetype="application/json")` 加 python sink_exclude）、过宽收窄为接收者限定（`Random(`/`getInstance(`/`.set(`/`extend(`/`merge(`/裸 `Auth(`/`createClient(`）、补漏（nosql_injection 25 条 mongo sink、go math/rand 弱随机、php `$wpdb->`/`DB::` SQLi 与 `header('Location:` 跳转）；**补充库 fold 进内置后删除**，`rules/` 留空目录做可选覆盖；最终对最原始五基准基线 A/B——vfa/flask-xss/demo-java/探针**零丢失**，仅 vampi 27→4（23 条丢失逐条实证为 JSON Response XSS **FP**） |

---

## 五、基准与回归体系（怎么证明「没改坏」）

一切收窄落地的闸门，都是这套 A/B：

1. **五基准**：`vfa` / `flask-xss` / `vampi`（python）+ `demo-java`（java）+ 探针样例。
   `benchmarks/baseline_snapshot.py snapshot/compare`，canonical key `(vuln_type, source.file:line, sink.file:line)`，
   **BEFORE ⊆ AFTER = 零损失**（真实漏洞一条不少）。
2. **OWASP 全量回归**（BenchmarkJava，52120 文件）：`--per-file` 分块扫（内存铁律）。
   **基准 `2026-08-25-narrow-batch`：TPR 96.5%（TP 1365 / FN 50），findings 21281**。
   评分客观性口径 + FN 溯源清单见 `docs/OWASP漏报清单.md`。
3. **结果归档**：每轮回归存 `benchmarks/owasp/results/<date>-<label>/`，便于对比。

---

## 六、文档导航（哪个问题看哪个 doc）

| 你想问 | 看 |
|---|---|
| 怎么用 / 参数 | `README.md` + `examples/scan_demo.py` |
| 项目怎么设计的、讲给别人听 | `docs/项目进展介绍.md` |
| 还有哪些事没做、优先级 | **`docs/剩余工作清单.md`**（P0–P3 执行索引，权威入口） |
| 误报从哪来、怎么治 | `docs/误报面清单.md`（A–F 类）+ `docs/规则库审查报告-2026-08.md` |
| 漏报面排查（A–I 类 + 复现材料） | `docs/漏报面清单.md` + `docs/OWASP漏报清单.md` + `docs/结构性漏报盲区.md` |
| 某个收窄批次怎么验证的、为什么敢落地 | `docs/对抗审查确认清单.md`（F6-java/python 等逐批记录） |
| 大项目卡死 / 性能 | `docs/性能分析-大项目卡死.md` + `docs/误报面清单.md` 性能类 |
| 扫描流程细节 | `docs/扫描流程阶段清单.md` / `docs/扫描流程排查报告.md` |

**读文档顺序建议**：先 `项目现状全景.md`（本文件）→ `剩余工作清单.md` → 按需进误报/漏报面清单。

---

## 七、当前状态速查

| 指标 | 数值 |
|---|---|
| findings 面 | **24571 → 20751**（误报持续收缩，真实漏洞零损失） |
| OWASP TPR | **96.5%**（TP 1365 / FN 50，基准 `2026-08-25-narrow-batch`） |
| 引擎能力 | 3 语言 × 28 漏洞类别 × 9 框架提取器，跨文件调用图 + def-use + CFG 支配 |
| 消费侧工具 | `dedup_findings.py`（归并复核）、`baseline_snapshot.py`（A/B 回归） |
| 未提交 | 无（P1-5/P1-8 均已提交，见四·E） |
| 已知边界 | 未做语义级命名/反射/动态分析；跨函数 CFG 不展开；盲区默认只含 `endpoint_no_source` |

**下一步**：P0 + P1 全部落地（P1-7/P1-6/P1-4/P1-5），进入 **P2（FN / 机制修复）**：F1 重载 `_uid` 塌缩、F2 参数合成赋值跨度、F3 `_word_in_text` 剔除字面量、N1 去重键加参数名、M1 BFS 早停、误报面 F def-use 遮蔽错配、误报面 E 桥接可达性收窄、漏报面 B/C/H——每项改动面大，各自独立回归。
