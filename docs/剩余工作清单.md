# 剩余工作清单（2026-08-25 快照）

> 所有待办按优先级排序。改动 findings 面的项必须过**缺陷平衡铁律**
> （五基准 A/B + OWASP 回归，真实漏洞零损失）才落地；P0 消费侧项
> （工具/开关）不改变 findings，无需回归。
> 清单由 TODO.md / 对抗审查确认清单 / 误报面清单 / 漏报面清单 / 规则库审查报告
> 汇总而来，各来源是权威细节，本文档是执行索引。

## P0 — 低风险高价值，可立即做（不改变 findings 面）

### 1. `dedup_findings.py` 复核去重工具

- **来源**：TODO P2 首条（用户 2026-08-21 建议）
- **动机**：全量 21281 findings 无法逐条看；findings 是「候选池」不是「漏洞清单」，先降维再复核。
- **做法**：输入 full/canonical 报告 → 按 `(file, line, vuln_type)` 归并 sink 位置的复核清单（TSV/JSON），每条附代表路径 1 条 + 命中计数 + 类别 + 严重级，按严重级倒序。
- **附带**：①按 vuln_type / 按文件聚合分布统计；②「已知误报模式」打标与过滤（如 `encode 了还报 XSS`、`绑参了还报 SQL`，同一模式一次判定整类标记）；③对外交付只给 top N 详情 + 汇总表。
- **前置**：先用 `owasp-merged.json` 做一次 FP 普查（951 条按 类别×根因 归类成表）当收窄基线。
- **风险**：零引擎风险，无需铁律回归。

### 2. `uncovered_sink` 显式开关

- **来源**：TODO P2；`cpg/discovery.py:325` 已有 `find_uncovered_sinks()`
- **做法**：`scan(uncovered_sinks=True)` / CLI `--uncovered-sinks` 显式开启（默认不产出，避免真实项目噪声爆炸）。
- **风险**：零引擎改动。

### 3. 方向修正批 · 删 Python `html.unescape(` 出 sanitizer

- **来源**：规则库审查报告方向修正批
- **现状**：`taint_rules.yaml:1209` python sanitizers 含 `html.unescape(`
- **理由**：unescape 是**解码还原**不是净化——把用户可控内容解除转义，性质反了。
- **回归**：五基准 A/B（sanitizer 增删可能影响 findings）。

## P1 — FP 收窄（先实证零 FN 损失再落地）

### 4. java 防御函数当 sink → 移 sanitizer

- **来源**：误报面清单 A 类剩余 + 规则库审查方向修正批
- **现状**：
  - `taint_rules.yaml:3189` java/path_traversal `getCanonicalPath(`（查路径非写文件）
  - `taint_rules.yaml:3423` java/auth_bypass `hasRole(`（鉴权判断非绕过）
- **Python 侧**：`url_for`/`escape`/`path.join` 已在历史批次处理。
- **回归**：五基准 + OWASP（java 侧改动）。

### 5. java sql `.execute(`/`.update(`/`.insert(` 泛方法名收窄

- **来源**：误报面清单 B 类 + TODO P3
- **现状**：`taint_rules.yaml:3012/3044/3057` java/sql_injection
- **难点**：MyBatis 档最难；须证实精确模式覆盖（如 `sqlalchemy.update(`）或接受保留。
- **回归**：五基准 + OWASP。

### 6. `auth_bypass` 类别重构（只留真 bypass 形态）

- **来源**：规则库审查方向修正批
- **现状**：python sinks/auth_bypass 12 裸：`Depends(`/`HTTPBasic(`/`OAuth2PasswordBearer(`/`Permission(`/`Security(`/`authenticate(`/`login(`/`login_user(`
- **理由**：FastAPI 参数类型是**声明不是漏洞**。
- **回归**：五基准（python 侧）。

## P2 — FN / 机制修复（改动面大，各自独立回归）

| # | 项 | 位置 | 性质 |
|---|---|---|---|
| 7 | **F1** 重载方法 `_uid` 塌缩（加 start_line） | graph.py | 图结构修复 |
| 8 | **F2** 参数合成赋值整函数跨度 | dataflow.py:156-164,184 | 数据流桥接修复 |
| 9 | **F3** `_word_in_text` 剔除字符串/注释字面量 | graph.py:232-247 | 词边界误判 |
| 10 | **N1** 去重键加参数名 | analyzer.py:319-325 | FN 修复（加 finding） |
| 11 | **M1** BFS 早停 | analyzer.py:517-574 | 会改变保留的 5 条路径 |
| 12 | 误报面 F：def-use 遮蔽错配（作用域化，34.2% 候选） | def-use 分析 | 唯一确证真错误，纯假连通零召回代价 |
| 13 | 误报面 E：桥接可达性收窄（type-gated） | 桥接 | 需 7 万文件扇出数据 |
| 14 | 漏报面 B/C/H：调用图盲区 / 返回值追踪 / 跨行桥接 | callgraph/dataflow | 待实测 |

## P3 — 功能增强 / 长期

| # | 项 | 说明 |
|---|---|---|
| 15 | 配置类漏洞 | `@Value`/properties/env 硬编码密钥（`hardcoded_secret` 配置侧） |
| 16 | 字段敏感/对象属性级污点 | `obj.userInput` 与 `obj.isAdmin` 区分传播 |
| 17 | JSON 链/链式方法返回值 | `JSON.parseObject(x).getString(...)` 继续传播 |
| 18 | 反射/动态特性 | `Class.forName`/`invoke` 模式化兜底 |
| 19 | 测试体系 `tests/` | 把 `/tmp/sanittest`/`posittest`/`dftest` 固化为回归用例 |
| 20 | BFS 性能优化 | 按 sink 反向 BFS 剪枝 / 同源合并 |
| 21 | 多语言 go/php 引擎 + 冒烟矩阵 | 规则已备在 `rules/go.yaml`/`php.yaml` |
| 22 | ORM 族保留项 | `.filter(`/`.order_by(`/`.select(`（SQLAlchemy 允许传原始字符串是真注入场景，留作未审计） |

## 已落地进度（供对照，勿重复）

- 低风险批次（2026-08-25，`24a6334`）：M2 流式序列化 / F5 盲区 / N2 jaxrs / N3 循环 / F4 注解
- 规则库收窄批次（`3abc99e`）：562 死模板删 + 81 处泛方法收窄（OWASP 零召回损失）
- python 收窄（`711812e`）：4 删 + 41 处接收者前缀收窄
- C1/C2/T1/T2/N4/F7 六项（`d5313ee`）
- F6-java（`190c705`）：17 裸注解收敛到分类器类别 + `params.` 接收者限定
- F6-python（`032bcd5`）：19 个无接收者访问器 → `request.X`/`req.X` 接收者限定；`.filename`/`.read(` 承重真输入保留
- 误报面从 24571 → 21281（FP 持续收缩，真实漏洞零损失）

## 铁律回顾

真实漏洞（FN）一条不能丢；误报（FP）能减就减。「不增加漏报」≠「不能减少 findings」——能证明是误报（桥接假连通/跨文件错配/宽 sink 误标/遮蔽错配）就直接砍。唯一硬约束：任何收窄/切断桥接/位置门控改动，先在既有基准上证明「真实漏洞命中一条不少」再落地。
