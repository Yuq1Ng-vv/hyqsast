# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# 虚拟环境 / 工具缓存
.venv/
.ruff_cache/
.pytest_cache/

# 生成的扫描报告
report.json

# MCP scan 默认落盘目录（<directory>/.hyqsast-report/）
.hyqsast-report/

# 本地机器相关的 Claude Code 权限配置（不入库）
.claude/settings.local.json

# 评分用的第三方 Real-Vuln Benchmark 仓库（嵌套 clone，不入库）
examples/Real-Vuln-Benchmark/

# OWASP 分块扫描产物（65MB+ JSON 报告，可经 benchmarks/owasp/ 重新生成）
benchmarks/owasp/results/

# xAST Benchmark 扫描产物（可经 benchmarks/ant/ 重新生成）
benchmarks/ant/results/

# xAST Benchmark 源码（第三方基准，已含独立 .git，不入库）
benchmarks/ant-application-security-testing-benchmark/

# OWASP Benchmark 源码（第三方基准，chunk_scan.py 首次运行自动 clone，不入库）
benchmarks/owasp-benchmark/

# 性能探针生成产物（可经 perf_gen_probes.py 重新生成）
perf_probes/
repro_py_fp/

# 离线依赖打包产物（scripts/build_vendor.py 生成，平台相关，不入库）
vendor/
